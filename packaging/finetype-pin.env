# The FineType release this repository stages into its artefacts, and the model
# registry revision the model comes from.
#
# TWO LINES, because the bundle has two sources and only one of them is the
# tag. A FineType release is to attach the extension, the taxonomy catalogue
# and a `finetype-model.json` naming the model; it is NOT to attach the model
# weights, which live in the model registry. So a tag alone does not determine
# the bytes a build stages and the revision has to be pinned beside it.
#
# THE TAG BELOW IS THE FIRST ONE THAT CARRIES ALL THREE, stated here because
# this is the file somebody bumping the pin reads. Measured against the live
# release on 2026-09-07: v0.6.59 publishes a `.duckdb_extension` and a `.sha256`
# for each of aarch64-apple-darwin, x86_64-apple-darwin,
# aarch64-unknown-linux-gnu, x86_64-unknown-linux-gnu and x86_64-pc-windows-msvc,
# plus taxonomy-schemas.json and finetype-model.json with their sidecars.
# v0.6.58 published none of the three — fetching finetype-model.json from it
# returns 404 — so a bump BACKWARDS from here stops at the fetch, which is the
# failure direction to want.
#
# THE STAMP AGREES WITH THE TAG FROM v0.6.59, and that took a FineType change.
# FineType committed `configure/extension_version.txt` holding `0.6.23` under a
# build rule with no prerequisites, so Make never regenerated a file that
# already existed and every extension it shipped stamped `0.6.23` whatever the
# tag — which the comparison scripts/package.sh makes between this tag and the
# extension's own trailer would have refused. The extension in v0.6.59 stamps
# `v0.6.59`; scripts/check-bundled-extension.sh strips one leading `v` from
# each side and reports them equal. Do not relax that comparison: it is what
# makes the pin a claim about bytes rather than about a name.
#
# Every consumer reads these through scripts/finetype-pin.sh —
# scripts/fetch-finetype-bundle.sh (which turns them into bytes),
# scripts/package.sh (which checks the bytes it staged carry this version) and
# .github/workflows/release.yml (which names the tag in the run summary).
# scripts/check-finetype-pin.sh runs each of those readers and refuses when any
# of them answers with something else.
#
# WHY A PIN AND NOT A RANGE. Two builds of the same brightfield tag must stage
# the same bytes, and a bump must be a line somebody read. The cost, accepted
# deliberately: this pin can rot silently — brightfield could ship a classifier
# a year behind FineType and nothing here would say so. There is no scheduled
# bump; moving it is a pull request like any other.
#
# BUMPING IS TWO LINES AND THEY MOVE TOGETHER. The tag decides which model NAME
# is staged, because the name is read out of the release's own
# finetype-model.json; the revision decides which registry commit that name is
# resolved at. A tag naming a model the pinned revision does not carry fails as
# a 404 during the fetch, which is the failure direction to want.
FINETYPE_TAG=v0.6.59
FINETYPE_MODEL_REVISION=94cda10a60c3e2eedb831d295c1db091836540c2
