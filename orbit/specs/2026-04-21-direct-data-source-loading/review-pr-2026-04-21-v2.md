# Pre-Merge Review

**Date:** 2026-04-21
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/direct-data-source-loading
**Spec:** orbit/specs/2026-04-21-direct-data-source-loading/spec.yaml
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite | PASS | 112/112 tests across 3 crates (brightfield-spec: 44, brightfield-conformance: 42, brightfield-sql: 21 + 5 integration) |
| AC coverage | 16/16 | See report below |
| `cargo check --workspace` | PASS | No warnings |

## AC Coverage Report (prefix: dfsql)

```
AC Coverage Report (prefix: dfsql):
  ac-01 (code):   ✓ Module layout verified: emit.rs, error.rs, lib.rs, render.rs, source.rs
  ac-02 (code):   ✓ dfsql_emit_error_variants
  ac-03 (code):   ✓ dfspec_parse_spec_path_populates_base_dir, dfspec_parse_spec_string_base_dir_is_none
  ac-04 (code):   ✓ dfsql_emit_sources_returns_one_ddl_per_data_entry
  ac-05 (code):   ✓ dfsql_parquet_emission, dfsql_parquet_http_url_passthrough
  ac-06 (code):   ✓ dfsql_csv_emission_with_extras, dfsql_csv_unknown_extra_warns
  ac-07 (code):   ✓ dfsql_json_emission, dfsql_geojson_requires_spatial_type, dfsql_spatial_emission
  ac-08 (code):   ✓ dfsql_duckdb_attach_emission, dfsql_duckdb_read_only_enforced
  ac-09 (code):   ✓ dfsql_inline_object_rows, dfsql_inline_array_rows, dfsql_inline_row_limit
  ac-10 (code):   ✓ dfsql_query_emission, dfsql_shorthand_emission
  ac-11 (code):   ✓ dfsql_canonicalise_is_deterministic, dfsql_canonicalise_sorts_by_view_name
  ac-12 (code):   ✓ dfconf_layer_2_missing_fixture_is_pending, dfconf_cli_layer_2_curated_all_pass
  ac-13 (code):   ✓ 10 .layer2.expected.sql files present (see F2 re legends.yaml)
  ac-14 (code):   ✓ All 10 .expected.yaml files show layer_2: pass
  ac-15 (code):   ✓ DEV-0002 in deviations.yaml + DEVIATIONS.md regenerated
  ac-16 (gate):   EXEMPT (gate — verified by test suite pass + cargo check clean)
  ac-17 (code):   ✓ dfsql_typed_without_file_is_invariant_violation, dfsql_opaque_is_invariant_violation, dfsql_unknown_extension_errors
  Coverage: 16/16 testable ACs covered, 1 exempt
```

## V1 Review Resolution

The v1 review (review-pr-2026-04-21.md) issued REQUEST_CHANGES with one MEDIUM finding:

- **F1 (MEDIUM): CSV warning channel missing** — RESOLVED. `emit_sources` now returns `EmitOutput { statements, warnings }` instead of bare `Vec<SourceDdl>`. Unknown CSV extras produce `ParseWarning::UnknownOption`. Test renamed to `dfsql_csv_unknown_extra_warns` and asserts `output.warnings.len() == 1`. Commit `cca28ae` addresses this.
- **F2 (LOW): DEV-0002 vs spec DEV-0001** — Acknowledged in v1 as needing no code change. Still correct.
- **F3 (LOW): DEV-0001 suppression narrowed** — Acknowledged in v1 as logically necessary. Still correct.

## Findings

### [LOW] F1: compact-rescue.txt committed to branch
**Category:** hygiene
**Description:** `compact-rescue.txt` (63 lines of planning/brainstorming text from a prior conversation session) is committed in the branch. It is not part of the implementation and should not be merged to main.
**Evidence:** `git diff main..HEAD --stat -- compact-rescue.txt` shows 63 insertions. Content is card-thickening brainstorming prose.
**Recommendation:** Remove from the branch before merge: `git rm compact-rescue.txt && git commit -m "chore: remove stray planning artifact"`.

### [LOW] F2: legends.layer2.expected.sql is empty (0 bytes)
**Category:** spec-wording
**Description:** AC-13 verification says "Each file is non-empty and contains at least one `CREATE OR REPLACE VIEW` or `ATTACH` statement." The `legends.layer2.expected.sql` fixture is empty because `legends.yaml` has no `data:` section — zero data sources means zero DDL. The implementation is correct (empty canonical output matches empty fixture, conformance passes), but the spec verification wording is slightly misleading.
**Evidence:** `wc -c legends.layer2.expected.sql` = 0 bytes. `grep data: legends.yaml` returns no matches. `dfconf_cli_layer_2_curated_all_pass` passes with `passed=10`.
**Recommendation:** No code change. The implementation correctly handles the edge case. The spec verification text could be tightened to "Each file corresponding to a spec with data sources is non-empty…" but this is a documentation nit, not a blocking issue.

### [INFO] F3: Deviation ID DEV-0002 vs spec constraint DEV-0001
**Category:** deviation
**Description:** Carried forward from v1. Spec says DEV-0001; implementation uses DEV-0002 because DEV-0001 was already taken. Correctly documented in progress.md as a detour. No action needed.

### [INFO] F4: DEV-0001 suppression scope narrowed
**Category:** scope
**Description:** Carried forward from v1. Layer 2 removed from DEV-0001's `conformance_layers_suppressed` (was `[2, 3, 4]`, now `[3, 4]`). Logically necessary since layer 2 is now active and DEV-0001's rendering deviation does not affect DDL emission. progress.md detour log covers this. No action needed.

---

## Honest Assessment

This implementation is ready to merge after removing the stray `compact-rescue.txt` file. All 17 acceptance criteria are satisfied, all 112 workspace tests pass, and the v1 review's substantive finding (missing CSV warning channel) has been cleanly resolved in the follow-up commit.

The architecture is sound: `brightfield-sql` is a pure-function emitter with no I/O or DuckDB runtime dependency, cleanly separated into dispatch (`source.rs`), entry point + types (`emit.rs`), error types (`error.rs`), and canonicalisation (`render.rs`). The conformance layer correctly transitions from stub to active gate for all 10 curated corpus specs. The deviation registry is properly maintained with DEV-0002 documenting the ATTACH READ_ONLY safety divergence.

The only action item is removing `compact-rescue.txt` — a trivial cleanup that does not affect any code or test.
