# review-pr — cfs3 cycle 1

**Date:** 2026-04-29
**Reviewer:** cold-context forked agent
**PR:** https://github.com/meridian-online/brightfield/pull/5
**Branch:** rally/cross-filtered-selections-interactors @ bccc126 (HEAD; impl is de7acb3)
**Base:** rally/reactive-parameters-runtime @ 69ed0e4
**Spec:** orbit/specs/2026-04-29-cross-filtered-selections-interactors/spec.yaml v1.2
**Verdict:** APPROVE

## AC traceability

| AC | Test | Verified? | Notes |
|----|------|-----------|-------|
| ac-01 | `cfs3_ac01_clear_selection_removes_contributor` (engine/lib.rs:2979) | yes | Builds a session with two contributors writing to `$brush` from `root/plot[0]` and `root/plot[1]`, then `clear_selection("brush", contrib_a)`. Asserts (a) the remaining contributor list is `[(contrib_b, _)]` (length 1, identity check), (b) results vec has exactly one entry, (c) the entry is `Ok` and the RecordBatch list is non-empty (re-execution actually fired). All four sub-clauses (a..d) of the spec verification text are covered. |
| ac-02 | `cfs3_ac02_clear_selection_unsubscribed_silent` (engine/lib.rs:3048) | yes | Two-part test: (a) `clear_selection("does_not_exist", path)` on a fresh session — empty result vec, `current_selections()` empty; (b) populates one contributor, then `clear_selection("brush", stranger)` for a path not in the list — empty result vec, contributor list field-equal to the snapshot. No panics. Sub-clauses (a) and (b) of the spec verification text are both covered. |
| ac-03 | `cfs3_ac03_click_outside_active_brush_clears` (chart_view.rs:541) | yes | Three sub-cases driven through `commit_brush_clear` against a `RecordingDispatcher`: (a) `Idle` → exactly one `clear` call recorded with `(name="brush", contributor=root/plot[0])`, results stub-empty, next state `Idle`; (c) zero-area `Brushing` (start to start+0.1px, within `ZERO_AREA_EPSILON=0.5`) → exactly one `clear` call, next state `Idle`; (d) non-zero `Brushing` (20,30→120,230) → zero `clear` and zero `dispatch` calls, state preserved. Sub-clause (b) is the assertion-of-(name, contributor) on the recorded clear, threaded through (a). All four sub-clauses covered. |
| ac-04 | `cfs3_ac04_plot_drives_multiple_selections` (chart_view.rs:603) | yes | Constructs a 100×200 brush rect and two bindings (`$a` IntervalXY + `$b` IntervalX) on `root/plot[0]`. Drives `commit_brush_release_multi` with a `RecordingDispatcher`. Asserts (c) `dispatcher.calls.len() == 2`, (d) names contain both `$a` and `$b` once, AND the `$b` (IntervalX) call's predicate is a 2-clause `And` whose every Expr references `speed` (x col) and none reference `delay` (y col) — i.e. the kind-compatibility filter discarded the y-range for the IntervalX binding. Aggregated return shape is `Vec<(String, Vec<...>)>` per the spec carve-out. Lifted-helper boundary, no GPUI simulation, matching the cfs2_ac11 / rpw3_ac12 precedent the spec calls out. |
| ac-05 | `cfs3_ac05_brushable_bindings_built` (analysis.rs:1942) | yes | YAML fixture has one `intervalXY` interactor writing `as: $brush`, one `panZoom` interactor (filter-out target), and a `dot` mark on `(speed, delay)`. After `analyse_spec`, `brushable_bindings.len() == 1` (panZoom excluded). The single entry's fields equal exactly: `interactor_path = root/plot[0]/interactor[intervalXY]` (using `kind.wire_name()` per analysis.rs:910), `parent_plot = root/plot[0]`, `selection = "brush"`, `kind = IntervalXY`, `channels.x = Some("speed")`, `channels.y = Some("delay")`. All five fields field-equal asserted; F1's path-convention fix from review-spec cycle 1 confirmed. |
| ac-06 | `cfs3_ac06_brushable_binding_to_brush_binding` (chart_view.rs:681) | yes | Constructs a `BrushableBinding` (spec-side) with `interactor_path=root/plot[0]/interactor[intervalXY]`, `parent_plot=root/plot[0]`, `selection="brush"`, `kind=Spec::IntervalXY`, `channels={x: "speed", y: "delay"}`. Calls `(&binding).into()` to invoke `From<&BrushableBinding> for BrushBinding`. Asserts every field of the resulting UI-side `BrushBinding` matches its source verbatim — `selection_name="brush"`, `contributor=root/plot[0]` (= parent_plot), `kind=BrushKind::IntervalXY`, channels both `Some(...)`. The conversion lives in brightfield-ui (chart_view.rs:286-298) — layering preserved. |
| ac-07 | `cfs3_ac07_param_change_preserves_selection` (engine/lib.rs:3109) | yes | Spec has `params: { threshold: {value:0}, brush: {select:intersect} }` and a dot mark `filterBy: $brush`. After `propagate_selection("brush", root/plot[99], "x >= 2")`, snapshots `current_selections()`, then `propagate_param("threshold", Float(5.0))`. Asserts (d) `current_selections() == &snapshot` (HashMap equality, not just length) AND (e) re-emitting the mark with the same selections vec produces SQL whose upper-case form contains `"WHERE"` and whose body contains the literal `"x >= 2"` brush predicate text. Both halves of the spec verification text covered. |
| ac-08 | `cfs3_ac08_propagate_param_does_not_clobber_selection_state` (engine/lib.rs:3186) | yes | Populates one contributor on `$brush`, snapshots `current_selections().clone()`. Calls `propagate_param("threshold", Float(1.0))` (first-walk) then `propagate_param("threshold", Float(2.0))` (re-entry). Final assertion: `current_selections() == &snapshot` — the HashMap as a whole is unchanged after both walks. The pre-snapshot `assert!(!snapshot.is_empty())` precludes the trivial empty-equals-empty case. Rally seam pinned. |
| ac-09 | `cfs3_ac09_brush_kind_point_constructs` (brush.rs:296) + `cfs3_ac09_non_brushable_kinds_excluded` (analysis.rs:1986) | yes | Two test functions sharing the `cfs3_ac09_*` prefix — F10 in cycle 1 reframed sub-clause (c) to live spec-side using a panZoom fixture, avoiding cross-crate enum coupling, and the spec verification text explicitly accepts the split. (a) sealed-enum coverage: `BrushKind::Point` is `!=` to all three interval variants; (b) `point_predicate("category", "'Athletics'")` returns `Predicate::Expr` whose body contains both `"category"` and the literal `"'Athletics'"` (quoting preserved verbatim — F7's contract honoured), AND `point_predicate("count", "42")` produces a body containing `"42"` but NOT `"'42'"` (helper does not quote numerics); (c) panZoom-only plot fixture analysed → `analysis.brushable_bindings.is_empty()`. All three sub-clauses covered. |

## Rally seam ac-10 verification

- **Check 1 (`cargo test -p brightfield-engine -p brightfield-spec -p brightfield-ui -p brightfield-render`):** PASS. 13 test suites, all green: 52 + 78 + 92 + 2 + 1 + 1 + 6 + 1 + 53 = 286 unit/integration tests pass; 0 failed; 0 ignored. Plus four empty doc-test suites. No suite failed or skipped.

- **Check 2 (`cargo tree -p brightfield-render | rg gpui`):** PASS. Zero matches (rg exits 1). brightfield-render carries no gpui dependency — Vello-only render seam intact.

- **Check 3 (signature-fingerprint diff against `origin/rally/reactive-parameters-runtime`):** PASS. The exact rg pattern from spec ac-10 step (3) — covering `propagate_param`, `topological_descendants`, `ParamDispatcher`, `SliderBinding`, `commit_slider_release`, `SliderState`, `propagate_selection`, `BrushBinding`, `brush_rect_to_predicate`, `emit_query`, `emit_query_with_passes`, `InteractorBinding` — produces zero hits after filtering hunk-header lines (`@@`). One incidental contextual `@@` line for `pub fn brush_rect_to_predicate` appears in the diff because adjacent code in brush.rs was modified (the `Point` branch added inside the function body), but the function's signature line itself is unchanged. The rpw3-shipped surfaces and v1/v2 untouched surfaces are all signature-stable. The intentional carve-out (`on_mouse_up_with_dispatch` parameter type and return shape, per ac-04) is correctly excluded from the pattern list.

- **Check 4 (test-function counts):** PASS.
  - `rg -n '\bfn cfs_'  crates/ | wc -l` → 12 (matches baseline 12).
  - `rg -n '\bfn cfs2_' crates/ | wc -l` → 16 (matches baseline 16).
  - `rg -n '\bfn rpw3_' crates/ | wc -l` → 16 (matches baseline 16).
  - New `cfs3_*` functions tracked separately: 10 (= 9 code-AC tests + 1 sub-clause split for ac-09(c) per spec F10), all enumerated in the AC-traceability table above. No cfs/cfs2/rpw3 function added, removed, or renamed.

## Constraint compliance

- **Coordinator surface (`Session::clear_selection`, no `propagate_*` signature change).** `clear_selection` lives at engine/lib.rs:354-427 with the signature the spec mandates: `fn(&mut self, name: &str, contributor: ComponentPath) -> Vec<(usize, Result<Vec<RecordBatch>, EngineError>)>`. The body is the linear-scan find-and-remove pattern at lib.rs:359-374, then a copy of the dispatch loop (lib.rs:376-426) symmetric to `propagate_selection` at lib.rs:262-335. The duplicate was permitted by the impl-notes ("a duplicate is also acceptable for clarity"). `propagate_selection` and `propagate_param` signatures are byte-identical to the rpw3 baseline (verified by ac-10 check 3).

- **Point-selection driver — chart-side click-to-point deferred.** `BrushKind::Point` lands at brush.rs:32 + `point_predicate` at brush.rs:134-136. The brush-rect-to-predicate match at brush.rs:91-123 returns `Predicate::True` for the `Point` arm (no rect-driven path), and `build_brushable_bindings` at analysis.rs:1014-1071 filters via `BrushKind::from_interactor_kind(kind)` which returns `None` for any non-{IntervalX, IntervalY, IntervalXY} variant, so Point can never appear in the chart_view dispatch input. `cfs3_ac09_non_brushable_kinds_excluded` proves this property without depending on the spec-side mirror enum naming.

- **Multi-binding shape.** `ChartView::on_mouse_up_with_dispatch` at chart_view.rs:131-163 takes `bindings: &[BrushBinding]` and returns `Vec<(String, Vec<(usize, Result<...>)>)>`. The `BrushBinding` struct at chart_view.rs:171-180 is unchanged in shape — multiplicity lives at the call site, per Decision 3. `commit_brush_release_multi` (chart_view.rs:198-227) is the lifted helper; `commit_brush_release` (chart_view.rs:237-249) is preserved as a single-binding wrapper that calls `commit_brush_release_multi` with `slice::from_ref(binding)` and pops the singleton aggregate — cfs2_ac11 stays green.

- **Brushable-binding metadata as a derived view.** `analysis.brushable_bindings: Vec<BrushableBinding>` is added at analysis.rs:1114; `interactor_bindings` (the v1 surface) is structurally untouched (count test `cfs_ac08_interactor_bindings` at analysis.rs:1836 still passes — it counts `interactor_bindings`, not `brushable_bindings`). `BrushableBinding` at analysis.rs:988-1001 carries exactly the five fields the spec mandates. The `BrushKind` and `ChannelColumns` mirror types at analysis.rs:946-979 prevent the reverse dependency; conversion lives at the consumer (chart_view.rs:286-308 — `From<&BrushableBinding> for BrushBinding` plus `brush_kind_from_spec`).

- **No reverse dependency from brightfield-spec on brightfield-ui.** Verified: `grep -r "use brightfield_ui" crates/brightfield-spec` → no matches. The mirror enums in analysis.rs (`BrushKind`, `ChannelColumns`) honour the layering rule.

- **Persistence semantics — brush persists verbatim across param changes.** Verified by `cfs3_ac07_param_change_preserves_selection` (positive: brush WHERE-clause re-emits) and `cfs3_ac08_propagate_param_does_not_clobber_selection_state` (regression: HashMap field-equality after two `propagate_param` calls). No domain check, no auto-clear, no warning channel — Decision 5 honoured.

- **Path convention `{path}/plot[i]/interactor[kind]`.** Verified at analysis.rs:1031-1044: `item_path = format!("{path}/plot[{i}]")`, then `interactor_path = format!("{item_path}/interactor[{}]", intc.kind.wire_name())`. `cfs3_ac05_brushable_bindings_built` asserts the literal `"root/plot[0]/interactor[intervalXY]"` against the produced path, pinning F1's review-spec fix.

- **Test naming 1:1 with ACs.** Nine code ACs map to ten `cfs3_acNN_*` test functions. The split for ac-09 (`cfs3_ac09_brush_kind_point_constructs` in brush.rs + `cfs3_ac09_non_brushable_kinds_excluded` in analysis.rs) is explicitly authorised by the spec verification sub-clause (c), which calls for an analysis-crate fixture to avoid coupling the spec-side test to the UI-side enum naming. Both functions share the `cfs3_ac09_*` prefix so the audit trace remains discoverable. Constraint substantively honoured.

- **Rally seam commitments.** The seam-fingerprint diff (ac-10 check 3) covers both rpw3-shipped symbols (propagate_param, topological_descendants, ParamDispatcher, SliderBinding, SliderState, commit_slider_release) AND v1/v2 untouched surfaces (propagate_selection, BrushBinding, brush_rect_to_predicate, emit_query, emit_query_with_passes, InteractorBinding) and produces zero hits. The intentional carve-out for `on_mouse_up_with_dispatch` (per ac-04, parameter type widened to `&[BrushBinding]` and return shape generalised) is correctly outside the pattern list.

## Decision pack alignment

- **Decision 1 (click-outside-active-brush is canonical; escape-key deferred):** honoured. `commit_brush_clear` at chart_view.rs:259-281 dispatches `clear` on `Idle` or zero-area `Brushing`. Escape-key memo filed at `orbit/cards/memos/2026-04-29-escape-key-clearing.md` per impl-notes paragraph 8 / F8.

- **Decision 2 (BrushKind::Point lands as type only; no chart-side click-to-point):** honoured. The `Point` variant exists at brush.rs:32, `point_predicate` exists at brush.rs:134, the rect-to-predicate match returns `True` for Point at brush.rs:122 (no rect-driven path), and `build_brushable_bindings` filters it out at analysis.rs:1034 (`BrushKind::from_interactor_kind` returns `None` for non-interval kinds, including Point). No chart_view dispatch path is wired.

- **Click-vs-drag — release-time zero-area branch (rationale shift documented in impl-notes para 7).** Honoured. `ZERO_AREA_EPSILON = 0.5` at chart_view.rs:187 is the boundary; `commit_brush_clear`'s `should_clear` test at chart_view.rs:267-274 fires on `Idle` OR `Brushing { start, current }` where `|start.x - current.x| < EPSILON` AND `|start.y - current.y| < EPSILON`. `on_mouse_down`'s body at chart_view.rs:67-77 is byte-equivalent to v2 (verified — no changes to the mouse-down brush-start gating), exactly per the rationale shift documentation. The cfs3_ac03 sub-case (c) walks the zero-area path with a 0.1px synthetic move, sub-case (d) walks the non-zero-area path.

## Findings

None.

## Notes

- **Test count for cfs3:** 10 functions for 9 code ACs (ac-01..ac-09), with ac-09 split across two crates per spec sub-clause (c) authorisation. The split is appropriate — coupling a spec-crate test to the UI-side `BrushKind::Point` naming would invert the layering rule the spec explicitly avoided. Audit traceability is preserved by the shared `cfs3_ac09_*` prefix.

- **commit_brush_release_multi return shape vs. spec ontology.** Spec ontology field "commit_brush_release_multi" describes the return as `Vec<(String, Vec<...>)>` per binding. Implementation matches at chart_view.rs:198-227. The single-binding wrapper `commit_brush_release` (chart_view.rs:237-249) returns `Vec<(usize, Result<...>)>` (the unwrapped second element of the singleton aggregate) — preserving the cfs2_ac11 surface byte-for-byte.

- **engine/lib.rs additions are pure-add.** `clear_selection` is a new public method; no existing method's body, signature, or visibility was changed. The cfs2_ac01..cfs2_ac12 and rpw3_ac04..rpw3_ac15 tests continue to pass without modification.

- **Memo filing follow-through.** The escape-key follow-up memo (`orbit/cards/memos/2026-04-29-escape-key-clearing.md`, F8 in cycle 1) is committed to the same branch — verified with `git log --diff-filter=A` against the file. Mirrors the Decision 5 memo pattern.

- **Compile-time discipline.** No new `unsafe`, no panics in non-test code, no allocations in hot loops — `clear_selection`'s body matches `propagate_selection`'s allocation pattern (one `selections` Vec, one `params_owned` clone, one `results` Vec).

- **Advisory (LOW, non-blocking):** the `RecordingDispatcher` test double in chart_view.rs grew a `clear_calls` field but the cfs2_ac11 test does not assert that `clear_calls` is empty after a successful brush release. Adding `assert!(dispatcher.clear_calls.is_empty())` to cfs2_ac11_on_mouse_up_dispatches_selection would tighten the boundary at zero cost. Not blocking.
