# Pre-Merge Review

**Date:** 2026-04-21
**Reviewer:** Context-separated agent (fresh session)
**Branch:** rally/mosaic-spec-driven-visualisation
**Spec:** orbit/specs/2026-04-20-mosaic-spec-driven-visualisation/spec.yaml
**Verdict:** APPROVE

---

## Test Results

```
| Check                          | Result | Details                                         |
|--------------------------------|--------|-------------------------------------------------|
| Unit test suite (src/*)        | PASS   | 35/35 passed                                    |
| tests/corpus_totality.rs       | PASS   | 1/1 (54 vendored YAML specs parse)              |
| tests/crossfilter.rs           | PASS   | 1/1                                             |
| tests/diagnostics.rs           | PASS   | 6/6                                             |
| tests/roundtrip.rs             | PASS   | 1/1 (corpus AST idempotence, 54 specs)          |
| cargo check --manifest-path …  | CLEAN  | No warnings                                     |
| cargo build                    | CLEAN  | Finished, no warnings                           |
| AC coverage (dfspec_ prefix)   | 14/14  | 1 doc AC exempt; all code/gate ACs have tests   |
```

Total: 44 tests pass, 0 fail. `cargo check` and `cargo build` produce no warnings — exit condition (`-D warnings`) met.

## AC Coverage Report (prefix: dfspec)

```
| AC    | Type | Status | Evidence                                                           |
|-------|------|--------|--------------------------------------------------------------------|
| ac-01 | code | PASS   | Cargo.toml: serde+derive, serde_yaml, serde_json, thiserror,       |
|       |      |        | indexmap+serde; zero miette match in grep                          |
| ac-02 | code | PASS   | dfspec_ac02_component_enum_is_sealed,                              |
|       |      |        | dfspec_ac02_mark_interactor_input_are_structs,                     |
|       |      |        | dfspec_ac02_paramref_to_wire; Config/PlotDefaults are newtype      |
|       |      |        | structs (F2 addressed)                                             |
| ac-03 | code | PASS   | dfspec_ac03_every_kind_has_status,                                 |
|       |      |        | dfspec_ac03_wire_lookup_round_trips                                |
| ac-04 | code | PASS   | dfspec_ac04_parse_spec_yaml_entry, …_json_entry,                   |
|       |      |        | …_path_unknown_ext                                                 |
| ac-05 | code | PASS   | dfspec_ac05_lift_surface_parametrised_string_form +                |
|       |      |        | _object_form iterate every LIFT_SURFACE_FIELDS entry (~95 × 2).    |
|       |      |        | F1 addressed — omissions now surface as test failures.             |
| ac-06 | code | PASS   | 5 grammar tests (params_outside_literals,                          |
|       |      |        | dollar_in_single_quoted_literal, double_quoted_identifier,         |
|       |      |        | line_comment, block_comment) + 3 sanity tests                      |
| ac-07 | code | PASS   | dfspec_ac07_meta_unknown_key_warns,                                |
|       |      |        | dfspec_ac07_mark_unknown_option_is_accepted (F3 addressed —        |
|       |      |        | locks post-D2 warning behaviour)                                   |
| ac-08 | code | PASS   | dfspec_ac08_unknown_mark_errors,                                   |
|       |      |        | dfspec_ac08_unimplemented_mark_warns_with_stub                     |
| ac-09 | code | PASS   | dfspec_ac09_variants_expose_span,                                  |
|       |      |        | dfspec_ac09_source_span_is_plain_struct; ParseError enumerates     |
|       |      |        | all 9 required variants with Option<SourceSpan>                    |
| ac-10 | code | PASS   | 5 version-handling tests cover match/mismatch/absent/invalid       |
| ac-11 | code | PASS   | dfspec_ac11_paramref_serialises_to_dollar_form,                    |
|       |      |        | dfspec_ac11_ast_round_trip_idempotent (unit), and                  |
|       |      |        | tests/roundtrip.rs iterating all 54 vendored specs                 |
|       |      |        | (F4 addressed)                                                     |
| ac-12 | gate | PASS   | tests/corpus_totality.rs — 54/54 YAML specs parse without          |
|       |      |        | ParseError                                                         |
| ac-13 | gate | PASS   | tests/crossfilter.rs — SelectionNode, filterBy ParamRef, as        |
|       |      |        | ParamRef all asserted                                              |
| ac-14 | gate | PASS   | tests/diagnostics.rs — cases (a) yaml, (b) json, (d) unknown       |
|       |      |        | mark, (e) strict-context $; plus unknown_selection_resolution      |
|       |      |        | and mark_data_missing_from (SchemaViolation). Case (c) is          |
|       |      |        | superseded by D2; covered by                                       |
|       |      |        | dfspec_ac14_meta_unknown_field_is_warning_not_error in unit        |
|       |      |        | tests. F5 addressed.                                               |
| ac-15 | doc  | EXEMPT | crates/brightfield-spec/README.md covers (a)–(e): entry points,      |
|       |      |        | ParseOutput/ParseWarning, Option Z contract, version pin, v1       |
|       |      |        | non-goals                                                          |
```

## Verification of prior-cycle findings

Review cycle 1 (`review-pr-2026-04-21.md`) filed five blockers (F1 HIGH, F2 MEDIUM, F3 MEDIUM, F4 MEDIUM, F5 LOW). Each has been addressed in the current branch; progress.md records these as Detour D3. Confirmed:

- **F1** — `crates/brightfield-spec/src/parse.rs:1534-1592`: both parametrised tests now iterate `LIFT_SURFACE_FIELDS` and panic on missing or incorrectly-lifted fields. Running the suite produces 44 tests pass and the parametrised tests complete in <1ms, confirming all ~95 fields lift cleanly on both string and object forms.
- **F2** — `crates/brightfield-spec/src/ast.rs:63-135`: `Config` and `PlotDefaults` are now `pub struct …(pub IndexMap<String, SpecValue>)` newtypes with `Deref`/`DerefMut`, honouring ac-02's "struct" contract while retaining the open-bag shape D1 narrowed to.
- **F3** — `crates/brightfield-spec/src/parse.rs:1598-1637`: both ac-07 tests now lock the post-D2 behaviour (meta unknown key → warning; mark option bag unknown key → silent-accept).
- **F4** — `crates/brightfield-spec/tests/roundtrip.rs`: new integration test iterates the same 54-file corpus as `tests/corpus_totality.rs`, parse → serialise → parse, asserts AST equality per file. Test passes cleanly on all 54.
- **F5** — `crates/brightfield-spec/src/parse.rs:1643-1654`: `dfspec_ac14_meta_unknown_field_is_warning_not_error` explicitly captures the D2 adjustment and locks it against regression.

## Additional spot checks

- **No miette dependency.** `Cargo.toml` has no miette entry; `grep -R miette crates/brightfield-spec/src` returns only doc-comments referencing the library boundary.
- **SourceSpan is a plain struct.** `crates/brightfield-spec/src/error.rs:18-24` — `pub struct SourceSpan { offset: usize, length: usize }`, no trait magic.
- **ParamRef canonicalisation.** `crates/brightfield-spec/src/ast.rs:202-206` + the custom `Serialize` impls in `parse.rs` serialise `ParamRef` to `"$name"` string form unconditionally. Unit test `dfspec_ac11_paramref_serialises_to_dollar_form` pins this.
- **Sealed Component enum.** `crates/brightfield-spec/src/ast.rs:229-254` — no `Other` variant; all 9 variants named (Plot, HConcat, VConcat, HSpace, VSpace, Legend, Mark, Interactor, Input). The `dfspec_ac02_component_enum_is_sealed` test matches exhaustively — compile-fail guard against accidental catch-alls.
- **Sealed vocabulary enums.** `crates/brightfield-spec/src/vocab.rs` uses a `vocab_enum!` macro that generates `wire_name()`, `from_wire(&str) -> Option<Self>`, and `status() -> ImplStatus` for every entry. `from_wire` returns `None` for unknown names → parser raises `ParseError::UnknownName`. No `Other` variant anywhere.
- **Lift surface pin.** `crates/brightfield-spec/src/parse.rs:38-158` — `LIFT_SURFACE_FIELDS: &[&str]` is the module-constant source of truth; parametrised tests fail if anything is wired incorrectly.
- **Corpus README.** `crates/brightfield-spec/vendor/mosaic-specs/README.md` records upstream SHA `d4d41a3275dbd6bc7995e1d1a82b0be18769bbca` + path + refresh procedure.
- **Detours consistent with spec intent.** D1 (narrow `deny_unknown_fields` to Meta only), D2 (downgrade Meta unknown-field to warning after corpus evidence: 20/54 specs use `meta.credit`), D3 (review-cycle fixes) — each is corpus-driven, well-reasoned, and has tests pinning the resulting contract.

## Evaluation Principles — weighted assessment

```
| Principle                                    | Weight | Delivered                                | Score |
|----------------------------------------------|--------|------------------------------------------|-------|
| Totality                                     | 0.30   | 54/54 corpus; full vocab registry        | 0.30  |
| Diagnostic fidelity                          | 0.25   | thiserror enum with 9 variants + spans;  | 0.25  |
|                                              |        | 6 diagnostic cases covered               |       |
| Structural introspection                     | 0.20   | crossfilter test + parametrised lift     | 0.20  |
|                                              |        | surface (string + object forms)          |       |
| Round-trip fidelity                          | 0.15   | tests/roundtrip.rs asserts AST equality  | 0.15  |
|                                              |        | for all 54 corpus specs                  |       |
| Library boundary (miette-free)               | 0.10   | Confirmed; plain SourceSpan              | 0.10  |
| Weighted total                               |        |                                          | 1.00  |
```

## Exit conditions

- "All 15 ACs verified green on the current branch" — **MET**. 14 code/gate ACs have passing tests under `dfspec_` prefix; ac-15 is `doc`-type and is satisfied by `README.md`.
- "Vendored corpus README records the upstream commit SHA" — **MET** (`d4d41a3275dbd6bc7995e1d1a82b0be18769bbca`, 2026-04-12).
- "Round-trip test passes for the full vendored corpus" — **MET** (`tests/roundtrip.rs`, 54/54).
- "Crossfilter structural test passes with the three named assertions" — **MET** (`tests/crossfilter.rs` asserts SelectionNode, filterBy ParamRef, as ParamRef).
- "The crate compiles under `cargo check` with no warnings (`-D warnings` in CI)" — **MET** (clean build; workspace lints configured in `Cargo.toml`).

## Findings

None at blocking severity. Minor observations below are for awareness; they do not change the verdict.

### [INFO] Diagnostics tests don't assert `span.is_some()`

**Category:** test-gap (minor)
**Description:** The ac-14 spec constraint says each of cases (a)–(e) should carry `span: Some(_)` (all are detectable from source location). The six `tests/diagnostics.rs` test bodies verify the variant identity and field values, but don't assert `span.is_some()`. The `span()` accessor exists and is exercised by `dfspec_ac09_variants_expose_span`.
**Evidence:** `crates/brightfield-spec/tests/diagnostics.rs` — no `span.is_some()` assertions.
**Recommendation:** Non-blocking. A one-line addition per test would close the loop; consider adding when next touching this file.

### [INFO] D2 detour sits well within spec intent

**Category:** documentation
**Description:** D1 + D2 + D3 detours in `progress.md` are corpus-evidence-driven and internally consistent. The spec's constraint list still describes the pre-D1/D2 contract ("`deny_unknown_fields` on meta, config, plotDefaults" in constraints; ac-07 description still describes a `ParseError` for a `meta: { bogus: true }` spec). The delivered behaviour matches D2 (warning, not error) and is tested; the drift between spec-constraint text and delivered contract is a bookkeeping item for a follow-up spec amendment, not a code gap. `progress.md`'s detour section handles this recording responsibility.

---

## Honest Assessment

Cycle 2 is a clean APPROVE. The parser is a well-crafted first crate: sealed registry enums with `ImplStatus`, no `Other` catch-alls, `ParamRef` canonicalising to `$name` on serialisation, zero miette coupling, a full 54-file corpus that parses to AST and round-trips through serialise→parse back to the same AST, and a parametrised lift-surface test that surfaces regressions mechanically. The five findings from cycle 1 are addressed with named tests rather than incidental coverage, and the three detours (D1: narrow `deny_unknown_fields` to Meta only; D2: Meta unknown-key → warning; D3: lift-surface parametrisation + newtype Config/PlotDefaults + ac-07 tests + corpus roundtrip + ac-14(c) re-homing) are traceable from corpus evidence to code in every case.

The one residual item is bookkeeping: the spec.yaml constraint text and ac-07/ac-14(c) descriptions still describe the pre-D2 contract. This is recorded in `progress.md` and is a spec-amendment follow-up, not a code defect — the delivered behaviour is correct, tested, and consistent with Mosaic's real-world usage (20/54 specs declare `meta.credit`; narrowing to fatal errors on unknown meta keys would have been wrong for totality). Downstream consumers get the API they need; the card is ready to merge.

**Verdict:** APPROVE
