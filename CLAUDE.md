## Product Positioning

Brightfield targets analytics **authors** — the people who build dashboards and visualisations — much like Canva/Illustrator targets graphic designers. The primary user is an analyst who declares what they want to see in a Mosaic spec and gets an interactive, GPU-accelerated rendering.

Analytics **consumers** (people who view and interact with published dashboards) are a future audience. Good architectural choices now — portable specs via Mosaic, clean separation between authoring and rendering — ensure consumer-facing delivery can be enabled later without rearchitecting.

**Platform targets:** macOS, Linux, and Windows — the egui/eframe + wgpu window stack supports all three (Metal on macOS, Vulkan on Linux/Windows); macOS is the validated target today.

**Reference projects for data expression:** RillData (real-time analytics dashboards, DuckDB-native), MotherDuck (cloud DuckDB, query visualisation UX), apple/embedding-atlas (GPU-accelerated interactive data visualisation at scale).

## Planning

Feature cards, specs, and decisions for Brightfield are tracked centrally, outside this repository — this repo holds the code, not the planning substrate.

**This repo is public, and the planning substrate is not.** Identifiers from it — decision-record refs, task ids, milestone ids, acceptance-criterion shorthand, document ids, card ids, spec AC ids — resolve nowhere for a reader here and leak the shape of private work. Don't put them in code comments, doc comments, prose, commit messages, or PR text. If a pointer carried real meaning, write the actual rationale in plain English instead.

`scripts/check-public-hygiene.sh` catches **part** of this, and it is worth being exact about which part, because the rest is on you. It is `git grep` over the tracked files of the current checkout, so it sees code, comments and prose — and nothing else. It does **not** read commit messages, PR titles or descriptions, review comments, branch names, or history that is no longer in the tree, and those are historically where the leaks actually happened. Treat the script as a floor, not as coverage.

It needs no arguments and no Rust build — run it before pushing; it is also the first CI job (`.github/workflows/public-hygiene.yml`) and goes red in seconds. Identifiers embedded in Rust identifiers are caught too: a test function or temp-dir literal that carries a spec AC id is a leak like any other, and the fix is to rename it. If the gate flags something genuinely legitimate, tighten the pattern and add the innocent string to `scripts/public-hygiene-innocent-strings.txt` (a tracked fixture the gate must stay silent on) rather than reaching for `scripts/public-hygiene-allowlist.txt` — an allowlist entry that does not parse, or that no longer suppresses anything, is a hard failure. `scripts/check-public-hygiene-selftest.sh` is the gate's own regression test; run it after touching a pattern.

## Previously Shipped

- "First end-to-end render" — Spec → SQL → DuckDB → GPU render → native GPUI window. Shipped at 7cb7005 (2026-04-29).
- "Live reactivity" — Param widgets drive queries (slider #25), cross-filtered brush + point selections propagate across views (#27–#31). Shipped 2026-07-04.
- "MIT-clean shipping floor" — LICENSE + clean-room GPL stub patch + cargo-deny CI gate (#34); Linebender stack bump to vello 0.9/wgpu 29 (#35). Shipped 2026-07-05.
- "Frame the canvas" — WorkspaceView shell (title strip, presentation toggle via first GPUI action), window-hosted legends (swatch + gradient), live-path colorScheme. Shipped at 29fb46f (#38, 2026-07-05).
- Mark breadth instalments (ongoing): density/regression (#21/#23), raster (#32), rect family (#28), sequential colour scale → true heatmaps (#36).
- "Accelerate to the authoring workspace" — three concurrent tracks, shipped 2026-07-06 at 1d1b9f4: legend click-to-filter (#41), raster determinism (#42), the density mark family heatmap/cell/contour (#43), and the DockArea authoring workspace with YAML editor + sidebar on gpui-component (#45; supersedes #44).
- "Cross-filter render fidelity" — Launch-anchored widen-only scales (gestures read as filtering: axes/colours hold still, query-widening data stays visible), legend selected-state (dim non-active swatches from the engine slot), live renderer-config seam (per-mark scheme/bandwidth/thresholds survive rebuilds via MarkInput.renderer_override). Shipped at 5167a31 (#50, 2026-07-10); in-app confirmed by Hugh (axes static, dimming visible).
- "Workspace consolidation" — WorkspaceView pruned (the `p` action lives on in workspace_actions.rs); bottom Log dock (reload/save feedback history, seeded closed, old layouts backfilled); presentation removes/rebuilds the bottom dock (no 29px strip in consumer preview); stack-rooted dock items (at gpui-component pin b7e63cc2 the whole drag machinery gates on StackPanel parents — bare tabs are locked); "Dock at Bottom" menu-move bootstrap (a dock's only panel can never start a drag). Shipped at 00f617f (#52, 2026-07-11); in-app confirmed by Hugh (menu-move, bootstrap-then-drag, `p` round trip, log entry). Hexbin follow-up spec landed alongside (#51, planning-only).
- "Sidebar profiling" — Real DuckDB per-column profiles in the Data sidebar (type, row count, count/type-gated min-max/approx distinct/nulls) for every source including unconsumed ones; launch session pre-window + the watcher's throwaway off-thread session on reload (the sidebar un-freezes — coordinator's UI-thread session never touched); per-source failure isolation → Log Warning, no toast (an in-spec broken path lands on the DDL-rejection layer by design); engine-owned profile types (profile.rs) + gpui-free profile_model formatting; sidebar_model.rs deleted as superseded. Shipped at 18fc7ba (#53, 2026-07-11); in-app confirmed by Hugh (live profiles, hot-reload pickup of an unconsumed source, broken-source rejection keeps the sidebar).
- "Hexbin marks" — Aggregate-typed channels ({count:}/{avg: col}, shared with the self-aggregating cell); HexbinLowerer (pixel-space binWidth 20, plain-SQL axial/cube-round, in-band __bf_ hex geometry + raw extents, zero-span→plot-midpoint d3 parity, data.filter honoured incl. extent subqueries); HexbinRenderer with RAW-anchored domains (axis = data extent; the occupied-centre anchoring was falsified by review — mesh drifted up to a full cell behind a probe weakened to pitch-only); hexgrid dataless mesh exactly on-lattice via analytic reconstruction from the shared scales (1e-6 px centres-equality probe, non-default-binWidth + anisotropic cases); vocab swap to geo; KDE dense lattice (density.png sanctioned re-baseline). Review: 3-lens/14-agent workflow, 8 confirmed findings, Option A escalation, targeted checker PASS. Shipped at cd660de (#54, 2026-07-11); in-app confirmed by Hugh (mesh concentric to the edges, dense lattice).
- "Axis inset" — Edge-point trim: positional scale RANGES pull inward (DEFAULT_SCALE_INSET 5px on continuous linear/time ends) so a mark at a domain bound renders whole inside the frame clip instead of clipping to a sliver; the zero-baseline end stays flush (exempt via zero_baseline_channel(), the pinned end read from the scene-composed domain so a co-mark's negative contribution can't wrongly exempt it), band + augment-only axes get no default. Explicit Mosaic attrs (inset/xInset/yInset/xInsetLeft…yInsetBottom, most-specific-wins, literal-only, explicit-0 = the Mosaic-exact opt-out) resolve in brightfield-spec::layout::resolve_plot_insets; a non-numeric value earns ParseWarning::NonNumericInset. RANGE-SIDE ONLY — frame clip, axis lines and domains untouched, so scene.rs/axis.rs/inversion needed no changes (ticks/marks/grid/brush-invert inherit it; anchor_scale copies the launch range verbatim → survives the widen-only fold). Both ChartLayout models (render layout.rs, ui chart_layout.rs) carry the 4 resolved insets, pinned equal by a cross-model agreement test; resolved once at app assembly, carried to each ChartState, gated on hot reload via ChromeSnapshot (an inset edit or data-driven default flip → "restart to apply", like colorScheme). Review: 3-lens/9-agent workflow, 6 confirmed findings all fixed (biggest: zero_pinned_end was re-scanning the base column, missing literal/family/augment contributions). 28/28 examples re-baselined (27 moved + cell band-only unchanged, all inset-explainable) behind a before/after gallery. Deviation: Mosaic default is 0; Brightfield defaults 5px because it clips marks (opt out per plot with `inset: 0`). Shipped at 7c396f3 (#55, 2026-07-11); gallery-ack'd by Hugh ("the axis-inset looks good" — 5px default + area-fill watch-list gaps accepted, no veto).
- "Keyboard grammar" — Selection-first keyboard-first authoring: scope = where focus sits on the ComponentPath tree, bare verbs act on the focused node, a fuzzy Space palette carries discoverability. Framework-free semantic layer (new gpui-free crate `brightfield-keys`: registry-as-data + focus-tree / scope / palette / Esc-ladder state machines) with a keymap adapter that builds gpui bindings from it (shipped p/cmd-s binding sites untouched). Shipped surfaces: focus model (h/l/j/k nav, focus ring + breadcrumb, cmd-e toggle), `?` help + `/` focus-jump overlays, Space command palette (fuzzy, Ctrl-j/k nav, greyed reserved rows, runs a verb via canvas-refocus-then-dispatch), `c` = transient colour-cycle (recolours the LAUNCH Fill ramp — a renderer_override swap alone is inert — retaining bandwidth/thresholds, View-scoped), cmd-r reload (force-flag into the existing watcher). Rode along on the branch: persistent brush selection (Mosaic/Vega-Lite fidelity), the Altair→Mosaic interaction gap-map memo, and the stale SelectionResolution vocab-flag fix. Reviews: seam-map + adversarial workflows (9 + 7 agents), 4 confirmed findings folded (palette selection escaping the 12-row render window, save-spec dead-row → greyed, honest reload-transience docs, two hardened tests). Merged to main `9e12c64` (2026-07-12); in-app confirmed by Hugh (Space palette, c on heatmap, Ctrl-j/k). Deferred: cross-filter/authoring verbs (need a keyboard data-target) + structural verbs (need the command log) + the mouse-free screencast; known edge — a cycled scheme persists across reload (coordinator-staleness limitation, docs corrected).
- "Axis + plot titles" — Labelled charts so a multi-view dashboard is self-describing (Hugh's in-app "which chart is which" finding). Derived axis titles from the encoding (x below the tick labels, y rotated up the left margin via draw_text_rotated's quarter-turn CCW); Mosaic-style xLabel/yLabel overrides (literal-only, most-specific-wins, explicit-empty suppresses, non-string → ParseWarning::NonStringLabel; a `$ident`-in-text label lifts to an Expression at parse time and degrades to Derive — deferred past v1); per-plot `title:` above the frame (left-aligned, Observable-Plot parity; absent → no title, no reserved space). Titles are DATA INK: pure spec resolver decides Override/Suppress/Derive (resolve_axis_titles, beside resolve_plot_insets), the field name resolves render-side against the ChannelMap (first column-bound entry names the axis; interval x1/x2 → no title), and grown margins reserve a fixed band per present title (grow_margins, TITLE_BAND = TITLE_SIZE+8) composing with the axis-inset range pull without clipping. Threaded through BOTH ChartLayout models (render layout.rs + ui chart_layout.rs, pinned by a cross-model agreement test) + the ui runtime ChartState (grown margins reach hit-testing/brush inversion via set_margins_and_insets, preserved across resize — the HIGH spec-review finding) + the anchored live-rebuild path (LivePlot.titles re-emits; ChromeSnapshot gate carries ResolvedTitles). Reviews: /orb:review-spec (REQUEST_CHANGES → v1.1) + impl review (FIX-THEN-SHIP; y-rotation pinned at the render site via a glyph-run transform probe — no GPU; `$`-in-text degrade doc+test). 11 ACs, 784 tests. Every example PNG re-baselined behind a before/after gallery: 27 moved, 1 byte-identical (rect-cells — both axes interval → no derived title, the spec-named non-move). Filed alongside: the interactive-legend affordance, from the in-app finding that a bound legend is visually identical to a static colour key. Shipped at 723d71e (#56, 2026-07-13); in-app confirmed by Hugh (titles visible on dashboard + crossfilter).
- "Legend interaction fidelity" — Two Vega-Lite/Mosaic fidelity gaps in a BOUND (`as:` a selection) legend, both living in the one legend hover/hit-test/render-state path, so shipped as one UI-only card. AFFORDANCE: a bound legend was byte-identical to a static colour key (the only "it's live" cue was the selected-state dim, which appears AFTER a click) — added a pre-click cue to bound categorical legends only: a PointingHand cursor over the panel (the app's FIRST CursorStyle — `window.set_cursor_style`, paint-phase, seam sized so the draggable-brush card reuses it) + a hover hot-state on the swatch under the pointer (hover_emphasis lighten-toward-white, distinct from the 0.35 dim) tracked on an `Rc<Cell<Option<usize>>>` riding PlacedLegend like `pressed`, refresh-on-change only; gated by legend_is_clickable(binding_present && Scale::Colour). MULTI-SELECT: plain click still single-selects (apply_legend_click untouched); shift-click toggles a category into an OR'd union (Vega-Lite `toggle: event.shiftKey`) via apply_legend_toggle — the union rides Predicate::Or, which compile_selection already emits and ORs, so ENGINE/SQL/SPEC are byte-untouched (diff confined to render+ui); the member set is DERIVED from the engine slot (the F1 lesson) via plural legend_selected_categories (decompose the slot's Or members, intersect this legend's categories) so a foreign predicate sharing the slot contributes nothing and display can't disagree with the built union; one member collapses to a bare Expr (single-select parity, shared statement cache), members canonically sorted, last-member-out CLEARS (never Or([]) = FALSE = zero rows), shift+miss is a no-op. render/legend.rs grows render_colour_legend_at_selected to a selected SET + hovered index (emphasis colour/alpha-only so the path_data invariant holds under both selection and hover); legend_scene.rs + the raster cache key thread the set + hover; slot_expr now Display `{p}` not Debug. NO example-PNG re-baseline (headless dumps capture launch state — no selection, no hover). /orb:review-spec REQUEST_CHANGES → v1.1 caught a real contradiction (a shift-click foreign-Expr fold vs the membership derive) + the slot_expr Debug-vs-Display substring bug. 789 workspace tests. Filed alongside during discovery: conditional encoding (Mosaic highlight-first — on its own branch, not this PR). Shipped at 79342c6 (#57, 2026-07-13); in-app confirmed by Hugh (cursor + hover treatment, shift-click union).
- "Geo mark" — the LAST mark (completes the grammar-of-graphics library). `mark: geo` projects GeoJSON Polygon/MultiPolygon (a `type: spatial` ST_Read source or inline GeoJSON text) client-side as a stroked basemap or filled choropleth, plot frame suppressed. GeoLowerer canonicalises the geometry column to the reserved `geom` (ST_AsGeoJSON for spatial sources, passthrough for inline VARCHAR GeoJSON — hermetic, no extension); GeoRenderer parses (serde_json), projects each vertex (equirectangular default or US-tuned Albers conic), aspect-fits into synthesized Scale::Linear, draws one BezPath per feature; a defaulted suppresses_frame() trait method gates axes/grid off (zero impact on other renderers). `LOAD spatial` at bootstrap gated to spatial-source specs, non-fatal offline. Scope Hugh-locked: equirectangular + US-Albers (albers-usa AK/HI composite deferred). The live examples/live/us-states.yaml basemap filters to the contiguous 48 by FIPS id (`id < '57' AND id NOT IN ('02','15')`) so plain Albers fills the frame — the full us-atlas' AK/HI + 5 Pacific/Caribbean territories at ±145–170° balloon the auto-fit extent and crush the lower 48 into a corner (the deferred composite gap made vivid); .gitignore's examples/live/*.json so the user-supplied ~842KB atlas is never committed. Hermetic inline choropleth examples/geo.yaml is PNG-baselined; all 28 prior example PNGs byte-identical (additive). Review: 3-lens/11-agent, 3 confirmed all fixed (renderer hardcoded "geom" → a custom `geometry:` channel silently rendered blank; weak Albers test → independently hand-computed point; unconditional LOAD → gated). Built in parallel with conditional encoding under ultracode (isolated worktrees). Shipped at 2662062 (#58, 2026-07-14); in-app confirmed by Hugh (contiguous-48 Albers basemap + inline viridis choropleth). 806 tests.
- "Conditional encoding (highlight)" — Mosaic-native, discovered highlight-first (the `highlight` interactor, NOT Altair `condition`). `select: highlight, by: $sel` dims the non-matching marks (opacity/fill/fillOpacity overrides) when a selection is active. The render primitive (HighlightState / apply_highlight / ChartData.highlight) pre-existed but was inert (every site passed highlight: None); v1 = parse→analysis binding (`by:` lifts to a param ref; HighlightBinding/HighlightStyle + build_highlight_bindings) + a per-row membership projection (`SELECT *, (<pred>) AS __bf_selected`, evaluated IN-DB against the source OUTSIDE any filter so the mark keeps its full batch and dims rather than drops rows — splom's cross-column highlight works natively) + a widened apply_highlight (dot/bar/rect/text honour the overrides; the other 12 families stay no-ops). Empty selection → no projection → byte-identical at rest → NO example-PNG re-baseline. 6-lens/14-agent adversarial review caught a HIGH the all-`params:`-declared test corpus HID: as-bound-only selections (a `$range` born from `intervalX as:`, no `params:` block) never projected — a flagship highlight silently never dimmed — fixed by synthesising a default single-resolution node; + a sentinel non-excluding self_source (highlight must grey the brushed plot's OWN rows, unlike filterBy self-exclusion) + a per-mark (not plot-wide) aggregate guard. New live-DuckDB tests closed the coverage gap (as-bound + same-plot). Ships with examples/highlight.yaml (a same-plot brush-to-dim demo, added at eyeball time — highlight had shipped with no runnable example; splom/weather are vendored parse-corpus specs, not examples). LESSON: green tests can hide a silent feature-dead bug when the fixtures under-sample the input space. Built in parallel with the geo mark under ultracode. Shipped at 7328c6f (#59, 2026-07-14); in-app confirmed by Hugh (brush-to-dim + same-plot self-exclusion). 827 workspace tests.
- "Draggable/resizable brush" — extends the cross-filter; UI-only. Makes the persisted committed brush directly manipulable: grab the grey `Selected` rect's interior to translate, pull an edge/corner to resize (opposite side pinned, no inversion past it), position-dependent cursor (open/closed hand for move, resize cursors on edges/corners) via the `window.set_cursor_style` seam — picked per-paint off an `Rc<Cell<BrushRegion>>` (the legend `hovered_index` pattern). A new `Dragging` sub-state gives zero-delta-grab inertness + per-plot isolation; the cross-filter re-dispatches from the moved corners ON RELEASE (`redispatch_brushing_from` pure fn → `invert_pixel_brush`; live-drag re-query stays deferred). Folds the brush-clamp chore: a new margins-only `ChartLayout::frame_area()` lets a brush enclose a boundary dot whose 4px disc overflows the 5px axis-inset band (GLOBAL — affects crossfilter too). Handle glyphs are GPUI quads only (never in the Vello scene) → all 30 example PNGs BYTE-IDENTICAL; the empty-diff gate over spec/sql/engine/render holds (portable Mosaic grammar byte-untouched). Click-to-clear moved from click-anywhere → click-OUTSIDE the rect (more Vega-Lite-faithful). Review: /orb:review-spec 6-lens → v1.1 (9 findings), then 4-lens code review → 2 LOW (grab-cancel on a missed mouse-up reverts to the anchor; the clear path covers the in-flight Dragging), each w/ a headless test + a negative-control on the re-dispatch wire (silent-no-op defence). Shipped at 043a4aa (#60, 2026-07-14); in-app confirmed by Hugh (drag + resize re-filter on release). 839 tests.
- "Keyboard command-log" — the VisiData spine; un-greys the 5 reserved NeedsCommandLog verbs. Keyboard structural edits to the focused View — `m` change-mark-type / `a` add-mark / `e` set-channel / `d` remove-mark / `u` undo — applied LIVE, with an opt-in `cmd-s` COMMIT to disk + a CommandLog dock panel (2nd bottom tab; DOCK_STATE_VERSION 1→2). ENGINE SEAM (the load-bearing risk): structural edits change the SQL, so MarkInput-swapping can't work → net-new PUBLIC `Session::reload_spec` swaps the session's private spec/analysis/mark_index_map in place, REUSES the connection + registered views, invalidates the statement cache → execute_mark re-emits fresh SQL (proven by a live-DuckDB test); an applied edit is DURABLE across later cross-filter gestures. gpui-free `SpecEdit` reducer (4 variants) + `apply` by ComponentPath + snapshot `UndoStack` w/ commit barrier (brightfield-spec/src/edit.rs); palette argument-prompt overlay (ArgCollector) for a/e; pristine-buffer commit gate (a dirty editor buffer REFUSES commit w/ reason — never clobbers hand-typed YAML); within-plot confinement as a hard edit-time refuse-with-reason (pinned EQUAL to the reload gate by an agreement test). Reviews: /orb:review-spec 4-lens → v1.1 (10 findings), then THREE code-review layers (core 8, delta 7, polish). The silent-no-op lesson recurred TWICE: (1) build_fresh_mark_input hardcoded highlight_style/projection None → `u`/`d` stripped highlight + geo on rebuild (invisible — fixtures used only dot/density) → re-derived + survival tests (the delta review caught the FIRST survival test as vacuous is_some(), added MarkRenderer::projection() asserting Some(Projection::Albers)); (2) the IN-APP eyeball (Hugh) found m/a/e/d/u silently no-op'd on a STATIC spec (scatter: categorical fill, no selection) — CrossfilterCoordinator::new returned None unless a spec had a brush/slider/bound-legend/SEQUENTIAL-fill, so no coordinator drove the re-render (apply_command_edit skipped Phase 2 while Phase 1 mutated the working spec + logged the edit) → fixed POST-review with a `command_log_active` gate (every authoring-window plot gets a live coordinator; dump/PNG path untouched, passes false) + a constructor-level regression test (static scatter → None without the flag, Some with it). Empty selection at rest → 30/30 example PNGs byte-identical. DOCUMENTED (by-design) LIMITATIONS: transient edits (editor pane shows disk, canvas shows the working spec, until cmd-s reconciles — the approved transient/opt-in-commit model, both eyeballed-OK by Hugh); SetChannel lands durably only on a LABELLED axis; commit re-serialises canonically (reformats/drops comments); undo is a session snapshot stack (no cross-commit); set-channel column pick-list = union across all profiled sources; `d` refuses on a single-mark plot (never-empty guard). Shipped at 6e41f77 (#61, 2026-07-14); in-app confirmed by Hugh (scatter mark-cycle re-renders live, highlight/geo survive m→u). 886 tests.

- "Discrete input widgets" — the last Altair gap-map item (gap-map COMPLETE). `input: menu` (+ `style: radio|checkbox` riding the preserved-verbatim options bag — NO new InputKind vocab; `input: radio` stays a parse error by test; portable by construction, serialise round-trip pinned) binds `as: $param` and drives subscribers live through the slider's param loop (ParamDispatcher reuse; the falsified "no coordinator change required" slider.rs header corrected to trait-reuse + additive lanes: menu_bindings/commit_menu/apply_menu/any_menu). Options: literal typed list or derived via net-new public `Session::distinct_values` (ordered, NULL-excluded, cap 50 + Log warning; `execute_raw_sql` stays cfg(test)); default reconciliation PREPENDS (never snaps), strict-variant equality; checkbox synthesises [true,false], checked ⇔ param==first option; radio 22px rows (literal-options only; derived/overflow degrades to menu); every degrade path = one runtime Log warning + a working widget. Reload honesty: ChromeSnapshot widget slice gates menu-affecting edits to "restart to apply" (colorScheme precedent); resolution warnings append BEFORE the reload gates. Slider-parity construction: gpui-free menu.rs + thin menu_element.rs (popup = deferred+occlude) + vello resting twins on Meridian ink. Full drive: card → tabletop (4 forks, Hugh-accepted recommendations) → spec v1.2 (3 cold review cycles; catches incl. a bare-checkbox 3-way contradiction + the hot-reload staleness class) → impl → 5-lens/39-agent ultracode adversarial review (14 raw → 7 confirmed = 5 distinct, ALL fixed; headline: the open menu's box-click could never close the popup — gpui capture-phase click_away then bubble-phase toggle re-open, fixed via render-time was_open snapshot; + one AC had no test through PRODUCTION ChromeSnapshot::capture). 953 tests; 30/30 example PNGs byte-identical; machine gates (brightfield-sql crate + parse.rs file) empty. Shipped at f0f521e (#64, 2026-07-17); in-app confirmed by Hugh (menu/radio/checkbox filter live; flag-narrowing semantics verified against the filter clause). Deferred: search/table kinds, selection-valued menus + filterBy narrowing, live options re-query, widget label: rendering.

## Upcoming Sprint Candidates

- "Interaction grammar (Altair gap-map)" — COMPLETE: conditional encoding (#59), draggable brush (#60), legend multi-select + affordance (#57), and input widgets menu/radio/checkbox (#64) all shipped. Memo retired.
- "Keyboard grammar follow-ons" — the command log SHIPPED (#61), unblocking structural verbs m/a/e/d/u. STILL deferred: the cross-filter keyboard verbs f/gf/t (need a keyboard DATA-target — a keyboard-selectable mark/category to filter BY; the command log is a SpecEdit stream, not a data-target); the mouse-free screencast (Hugh records); command-log polish (editor live-preview of the working spec so canvas/pane agree pre-commit; set-channel source-narrowing to the focused plot's source; `d`-refusal wording; watcher-driven coordinator rebuild under an external disk edit). Seed: the VisiData keyboard-grammar memo.
- "Continuous slider drag" — UNBUILT. What merged (PR #26, 2026-07-16) was the plan; the feature itself was never written. Needs /orb:design: throttle vs debounce, query cancellation, async boundary.
- Chores: (brush-clamp vs mark-radius chore FOLDED into #60 via `frame_area()`.) data.filter family backfill (DensityLowerer/RegressionLowerer destructure `MarkData::From { source, .. }` discarding extras — silent filter drop; same fix shape as hexbin's F4: predicate into row WHERE + extent subqueries); spec_save temp-dir race (noisy under load — three sibling tests share temp_dir()/bf-spec-save-<pid>/ and the :186 scan flags ANY .bf-save- entry; fix = per-test unique subdirs or scope the scan); NULL-numeric-fill convention (opaque steelblue via DEFAULT_COLOUR across dot/cell/hexbin — future card, reads as a high value on blues schemes); RegressionLowerer stroke-group ORDER BY (jitter class from #42); coordinator refresh on hot reload (watcher swaps scenes only). (The gpui-era chores — upstreaming the gpui-component DockEvent two-liner, re-evaluating crates.io gpui — died with the shell cutover.)
