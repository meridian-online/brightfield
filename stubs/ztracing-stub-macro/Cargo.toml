[package]
name = "ztracing-stub-macro"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Pass-through #[instrument] attribute macro backing the ztracing stub."
publish = false

[lib]
proc-macro = true
