[package]
name = "brightfield-ui"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Framework-free chart interaction logic: brush/click/widget commit paths, chart state, and the live cross-filter coordinator"

[dependencies]
brightfield-render = { path = "../brightfield-render" }
brightfield-engine = { path = "../brightfield-engine" }
brightfield-spec = { path = "../brightfield-spec" }
brightfield-sql = { path = "../brightfield-sql" }
vello = "0.9"
kurbo = "0.13"
peniko = "0.6"

[features]
gpu-tests = []

[dev-dependencies]
indexmap = "2"
arrow = { version = "58", default-features = false }

[lints]
workspace = true
