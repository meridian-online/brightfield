rally: "spec foundation"
autonomy: guided
phase: complete
started: 2026-04-20T00:00:00Z
completed: 2026-04-20T18:00:00Z
cards:
  - path: orbit/cards/0001-mosaic-spec-driven-visualisation.yaml
    status: complete
    spec_dir: orbit/specs/2026-04-20-mosaic-spec-driven-visualisation/
    branch: rally/mosaic-spec-driven-visualisation
    worktree: main
    parked_constraint: null
  - path: orbit/cards/0002-mosaic-web-spec-portability.yaml
    status: complete
    spec_dir: orbit/specs/2026-04-20-mosaic-web-spec-portability/
    branch: rally/mosaic-web-spec-portability
    worktree: main
    parked_constraint: null
implementation_order:
  - orbit/cards/0001-mosaic-spec-driven-visualisation.yaml
  - orbit/cards/0002-mosaic-web-spec-portability.yaml
order_rationale: |
  Non-disjoint designs force serial order. Card 0001 produces symbols card 0002
  consumes: the Option Z vocabulary registry enum (Implemented | Planned |
  Unimplemented), the sealed AST node taxonomy with stable component identities,
  the round-trip serialisation invariant, and source spans on AST nodes. Card
  0002's preflight SupportReport reads status metadata directly from card 0001's
  stubbed AST nodes; the conformance runner's layer-1 gate depends on card
  0001's round-trip invariant; the deviation registry keys entries against
  stable identities card 0001 defines. Card 0002 never edits parser internals,
  but cannot land before those symbols exist.
