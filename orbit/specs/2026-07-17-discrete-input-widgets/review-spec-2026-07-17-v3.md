# Spec Review — Discrete input widgets (menu / radio / checkbox) — v1.2 re-review (cycle 3)

**Date:** 2026-07-17
**Reviewer:** Context-separated agent (fresh session, cold read from disk — no prior-cycle context beyond the review artifacts themselves)
**Spec:** orbit/specs/2026-07-17-discrete-input-widgets/spec.yaml (v1.2, cards: 0024)
**Prior cycles:** review-spec-2026-07-17.md (v1.0, REQUEST_CHANGES — 8 findings, all folded in v1.1) → review-spec-2026-07-17-v2.md (v1.1, REQUEST_CHANGES — 1 MEDIUM + 3 LOW; v1.2 claims all folded)
**Supporting artifacts read:** tabletop.md, orbit/cards/0024-discrete-input-widgets.yaml, both prior reviews, code verification against the working tree at `c166df3`. `git diff --stat 5760db9..HEAD -- crates/` is empty — no code moved since the v2 review's line-exact anchor sweep, so v2's verified anchors stand; this cycle re-verified the anchors the v1.2 amendments themselves cite.

---

## Review Depth

| Pass | Triggered by | Findings |
|------|-------------|----------|
| 1 — Structural scan | always | 0 (gate-AC check clean) |
| 2 — Assumption & failure | content signals (engine API, watcher boundary, machine gates) + prior-cycle carry | 1 LOW |
| 3 — Adversarial | composition check on the v1.2 amendments themselves (the v2-M1 class: do the new rules collide with each other or with standing ACs?) | 0 |

Gate-AC deterministic check (Pass 1, rule 5): diw-ac12 and diw-ac15 remain the two `ac_type: gate` ACs; both descriptions non-empty, non-placeholder, well over 20 chars. PASS.

---

## Prior-Finding Resolution Table

Each v2 finding checked against the v1.2 text AND re-verified against the code where the amendment makes a code claim.

| # | v2 finding | v1.2 resolution | Verified? |
|---|-----------|-----------------|-----------|
| **M1** | Literal-radio height formula × default-prepend rule collide: an unlisted param default renders one more 22px row than layout reserved (layout_component cannot see the params block), and every specified test passes over the overflow | **RESOLVED — the recommended option (a), implemented exactly.** New decisions_locked "RADIO ARM (symmetric post-prepend rule)": a `style: radio` whose POST-RECONCILIATION count exceeds its layout-time literal N degrades to menu presentation with a runtime Log Warning, with the honest rationale inline ("the prepend added a row layout_component never reserved — layout.rs:163 cannot see the params block") and the rarity framing ("fires only on the slider-clamp authoring-inconsistency class"). diw-ac14's degrade list gains the entry ("style: radio whose post-reconciliation count exceeds its layout-time literal N → menu presentation"), and its verification gains the composition test the v2 review demanded: "literal radio [a,b,c] + absent param default d → menu presentation + one warning (the geometry never overflows the 22·N + RADIO_CHROME_PAD rect)". | Yes — and the rule is geometrically sound, not just worded: the degraded menu is 32px inside a 22·N+10 rect, which is ≥ 32 for all N ≥ 1 (N=1 is an exact fit) — "cosmetic slack, never overlap" is arithmetically true at the pinned constants. The trigger predicate is precise: the prepend adds at most one option, so post-count ∈ {N, N+1} and "exceeds" is unambiguous. diw-ac05 is correctly left untouched (layout still sizes at literal N — the only thing a pure spec fn at layout.rs:163 *can* do; the degrade closes the render side). Checked for knock-on collisions: the degraded-radio case composes with diw-ac17's widget slice (whether the slice stores declared or finalised style, a default-membership edit moves the resolved options and gates — honest either way) and with the checkbox arm (checkbox degrade is 200×32 → 200×32, no geometry question). |
| **L1** | Watcher-path resolution warnings appended AFTER the reload gates, so they drop exactly when a menu-affecting edit produces them; transport test only exercises the hot-swapped path | **RESOLVED — the review's first option (pre-gate append), taken with the test it asked for.** decisions_locked RELOAD POSTURE now ends: warnings "are appended BEFORE the same_layout/chrome_divergence gates (main.rs:1278/:1299 continues) — a gated 'restart to apply' reload co-surfaces the warning explaining why, instead of dropping it at the continue." diw-ac04 mirrors it ("appended exactly once, BEFORE the same_layout/chrome_divergence gates") and its verification adds the gated case: "warning-producing edit that also trips chrome_divergence → warning appended, then the gate fires". | Yes — anchors line-exact: the `continue`s sit at main.rs:1278 (same_layout) and :1299 (chrome_divergence) precisely. Feasibility re-verified in the watcher body: the run_pipeline return (which the warnings ride, alongside rebuilt_profiles/rebuilt_chrome) is destructured before :1265, and `feedback_log` is already threaded to both gate sites via notify_reload_rejection — the pre-gate append is a straight-line edit, no restructuring. Consistency with "exactly once" checked below (new-findings hunt, item a) — it holds. |
| **L2** | diw-ac17's "widget-neutral edits still hot-swap" was unconditional, but a Derived menu's resolved-options snapshot drifts with the data, converting any widget-neutral edit into restart-to-apply | **RESOLVED.** diw-ac17 now carries the qualifier essentially verbatim: "WITH the derived-options qualifier: under a Derived menu whose underlying data drifted since launch, ANY spec edit gates to restart-to-apply (the resolved slice moved; the hosted widget genuinely IS stale) — accepted, honest-conservative; snapshotting source/column instead would readmit the stale-options hot-swap this AC exists to close." Verification re-scoped to match: "a widget-neutral edit under UNCHANGED data does not [diverge]". | Yes — the qualifier keeps the resolved-list snapshot (the mechanism v1.1's H2 fix depends on) and documents the conservative edge instead of hiding it; the counter-rationale for the source/column alternative is carried, so a future implementer can't "optimise" the snapshot back into the hole. Consistent with out_of_scope bullet 3 (external data change shows on restart, not hot reload). |
| **L3** | (a) from_input's "surfaced reason" had no transport (slider-parity `Option<Self>` can't carry it); (b) diw-ac06's callsite enumeration missed crossfilter.rs's own five test ctor callsites | **RESOLVED.** (a) diw-ac01: "The return is WIDENED beyond slider parity to carry degrade reasons — (Option<MenuBinding>, Vec<reason>) or equivalent — so the gpui-free model stays the single source of the degrade decision and assembly only logs what the model surfaced" — the review's recommended option (widened return), stated as a deliberate parity deviation, not a drift. (b) diw-ac06 now reads "non-test callsite: main.rs:1643 — the authoring window; #[cfg(test)] sites: main.rs:2322/:3195 and crossfilter.rs's own :2223/:2231/:3011/:3082/:3170; the dump path never builds a coordinator". | Yes — grep over `CrossfilterCoordinator::new` returns exactly those eight callsites (five in crossfilter.rs, three in main.rs) and no others; the enumeration is now complete and correctly partitioned test/non-test, meeting the spec's own "claims about what changes name their sites" principle. One residual wording overreach in (a)'s parenthetical — see NEW L1 below; it does not reopen the transport question, which is answered. |

**Summary: all 4 v2 findings genuinely resolved** — each is the v2 review's own recommended option implemented with the demanded test, not a reword, and each was re-verified against the tree (radio-degrade geometry, gate `continue` lines, ctor callsite grep).

---

## New Findings

Hunt focus: what the v1.2 amendments themselves introduced.

**(a) Pre-gate append × "appended exactly once" across a gated-then-restarted sequence — checked, consistent.** A warning-producing edit that gates: one append (pre-gate), then the rejection notification — one watcher assembly pass, one append. The user restarts: the launch path re-warns via the main.rs:1569 precedent — a *new* assembly pass, covered by diw-ac04's explicit "warnings are per assembly pass by construction". Repeated watcher fires without a restart are likewise one append per pass. No sequence produces zero or double appends within a pass; the two claims coexist.

**(b) Widened from_input return × every slider-parity citation — checked, consistent.** diw-ac01 names the widening as a deliberate deviation ("WIDENED beyond slider parity") while keeping the behavioural parity claims it inherits (as_param-required, else-None). No other AC cites from_input's return shape; diw-ac16's layer rule (element/hosting call the model, no logic) is satisfied by the widened shape. One parenthetical overreaches — NEW L1.

**(c) Radio-arm degrade × diw-ac05/ac11/ac17 — checked, consistent** (see M1 row: geometry sound at the pinned constants, layout AC correctly untouched, widget-slice interaction gates honestly either way).

### [LOW] L1 — diw-ac01's "the diw-ac14 warnings ride this transport" overclaims: three of ac14's five degrade classes cannot be decided by from_input at all

**Category:** assumption
**Pass:** 2
**Description:** diw-ac01's widened return closes v2-L3(a) for the degrades from_input can see on a lone Input node: unknown style, derived-options radio, checkbox synthesis. But the parenthetical says "the diw-ac14 warnings ride this transport" — and diw-ac14's list also contains checkbox ≠2 POST-RECONCILIATION, radio post-reconciliation count > literal N, and cap-50 truncation, all of which depend on the param default or the distinct_values result, neither of which from_input's signature (`&Input`, slider.rs:90 parity) ever sees. Those three must ride the analogous widened returns of the gpui-free reconciliation/finalisation functions — which the constraints already place in menu.rs ("options-resolution logic live gpui-free in brightfield-ui/src/menu.rs") — not from_input's. The design is coherent; the sentence's scope is not.
**Evidence:** diw-ac01 parenthetical vs diw-ac14's five-entry list vs decisions_locked DEFAULT RECONCILIATION (prepend needs the param default, an assembly-time input) and OPTIONS SOURCE (truncation needs the distinct_values result); slider.rs:90 (`from_input(&Input)` — no params-block visibility).
**Proposed fix:** One clause: re-scope the parenthetical to "the from_input-decidable subset of the diw-ac14 warnings rides this return; the post-reconciliation degrades ride the same widened shape on menu.rs's resolution/finalisation fns". No mechanism change. This must not drive the verdict: the constraint already forces resolution logic into the gpui-free model, ac14's assembly-level tests pin the observable FeedbackLog entries regardless of which model fn surfaced them, and an implementer following the model-surfaces/assembly-logs pattern the spec repeats three times will land the right shape without noticing the ambiguity.

### Notes (non-findings, recorded for the implementer — none verdict-relevant)

- **chrome_divergence anchor is off by four**: spec says main.rs:650, the fn is at :646 (ChromeSnapshot :567 is exact). Pre-existing since v1.1; unambiguous in context.
- **decisions_locked header provenance**: the comment "tabletop 2026-07-17 … verbatim-faithful" now heads a list whose later bullets (CHECKBOX ARM, RADIO ARM, RELOAD POSTURE ordering) are review-cycle folds postdating the tabletop. metadata.version_history records the true provenance, so the record is recoverable — but a future cold reader of decisions_locked alone would misattribute them. A one-word header amendment ("tabletop … + review folds v1.1/v1.2") when the spec is next touched.
- **The gated-case transport test** will in practice need the watcher-side append-then-gate ordering extracted as a testable decision (the spawned watcher loop itself is not headless-drivable) — the same extract-the-pure-decision class the repo already uses for reload_notification. The AC's wording ("transport test pinning that run_pipeline returns the resolution warnings and the reload path appends them exactly once INCLUDING the gated case") is satisfiable under that reading; noted so the extraction reads as compliance, not weakening.

---

## Honest Assessment

The v1.2 fold is genuine and complete: all four v2 findings were resolved by implementing the v2 review's own recommended options — the radio post-prepend degrade is geometrically verified sound at the pinned constants (32px menu inside a ≥32px rect for all N ≥ 1), the pre-gate warning append is anchored line-exact to the two `continue`s it must precede and is a straight-line edit in the watcher body, the derived-drift qualifier keeps the snapshot mechanism while documenting its conservative edge, and the ctor callsite enumeration is now grep-complete. The adversarial pass this cycle specifically hunted the composition class that bit v1.1 (amendments colliding with each other or with standing ACs) and found nothing: pre-gate append coexists with per-pass single-append across gated-then-restarted sequences, and the widened from_input return breaks no parity claim. The single new finding is a one-clause wording overreach about which model function's return carries the post-reconciliation degrade reasons — a LOW an implementer following the spec's own layer rules would resolve correctly without noticing. Three cycles in, every gate is machine-checkable against the tree, every capability claim names a drivable surface, both stand-ins are the honest pixels-on-screen pair, and the recurring silent-no-op and stale-reload classes each have a named defence with a negative control. This spec is ready to implement.

**Verdict:** APPROVE
